version: "3.9"

services:
  xagent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: xagent-app
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      SPRING_PROFILES_ACTIVE: test
    restart: unless-stopped